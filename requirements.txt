# Philippine Hoya Clade Classifier — runtime dependencies for Streamlit Cloud
#
# IMPORTANT: NumPy must be 2.x (not 1.26.x).
# The model pickle stores an MT19937 BitGenerator using a legacy format.
# NumPy 1.26 raises "ValueError: ... is not a known BitGenerator module"
# when unpickling. NumPy 2.x has the backward-compat shim that handles it.
#
# Verified working combination: numpy 2.2.x + scikit-learn 1.7.x + Python 3.11.
 
streamlit>=1.30.0
numpy>=2.0,<3.0
pandas>=2.0.0
scikit-learn>=1.6,<1.8
plotly>=5.18.0
