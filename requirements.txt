# Philippine Hoya Clade Classifier — runtime dependencies for Streamlit Cloud
# scikit-learn is pinned to 1.6.x because the production model was
# pickled with 1.6.1; newer majors trigger InconsistentVersionWarning
# and can cause subtle numerical drift in predictions.
streamlit>=1.30.0
numpy>=1.24.0,<2.0
pandas>=2.0.0
scikit-learn>=1.6,<1.7
plotly>=5.18.0
